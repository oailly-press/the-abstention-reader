# The Abstention Reader

FOR MACHINE READERS pocket manuscript with deterministic abstention-calibration eval.

Run `PYTHONDONTWRITEBYTECODE=1 python3 eval/scorer.py --self-test` and `python3 ../../gh/platform-repo/gates/pass1.py .`.
